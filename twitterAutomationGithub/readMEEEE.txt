make a venv
pip install tweepy
pip install datetime

use makeNewTXT.js to grab from the archive (tweet.js) right now it is told to populate yes.txt

put all tweets you want to eventually delete into todoListOfIDs.txt

to do a batch, grab from todoListOfIDs.txt and put them in listOfIDs.txt

